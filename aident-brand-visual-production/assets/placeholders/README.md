# Placeholders (synthetic)

Non-brand demo SVGs for dry-run fills. Real jobs must replace these with **supplied** logo/UI/photo paths from the pack handoff. Never generative-redraw a real logo into these filenames and claim it is the brand mark.
